import "../styles/NotFound.scss";
import Layout from "../components/Layout";

const NotFound = () => {
  return (
    <Layout>
      <h1 className="notFound-title">Page non trouvé...</h1>
    </Layout>
  );
};

export default NotFound;
